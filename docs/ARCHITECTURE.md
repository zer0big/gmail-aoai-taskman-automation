# Email2ADO 아키텍처 설계 문서

## 📋 개요

이 문서는 Gmail 기반 이메일 자동화 시스템의 아키텍처를 **Azure Well-Architected Framework (WAF)** 5대 원칙에 맞춰 설계한 내용을 설명합니다.

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Email2ADO 자동화 시스템                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────┐    ┌────────────────────┐    ┌────────────────────┐      │
│  │  Gmail   │───▶│  Logic App Standard │───▶│  Azure DevOps     │      │
│  │  INBOX   │    │  (Stateful Flow)    │    │  Work Items       │      │
│  └──────────┘    └────────────────────┘    └────────────────────┘      │
│                           │                                              │
│         ┌────────────────┼────────────────┐                             │
│         ▼                ▼                ▼                             │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐                     │
│  │ Table Storage│ │ Azure OpenAI │ │ MS Teams     │                     │
│  │ (중복 체크)   │ │ GPT-4o       │ │ 알림 채널    │                     │
│  └──────────────┘ └──────────────┘ └──────────────┘                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

## 🎯 Azure Well-Architected Framework 적용

### 1️⃣ Reliability (신뢰성)

> **목표**: 장애 발생 시에도 시스템이 정상 동작하고 복구 가능

#### 적용 사항

| 항목 | 구현 내용 | 위치 |
|------|----------|------|
| **Retry Policy** | 모든 HTTP 액션에 Exponential Backoff 적용 | `workflow.json` |
| **중복 처리 방지** | Table Storage 기반 Message ID 중복 체크 | `Scope_Duplicate_Check` |
| **Graceful Degradation** | 일부 실패 시에도 핵심 기능 유지 | `runAfter: ["Succeeded", "Failed", "Skipped"]` |
| **상태 추적** | Processing → Completed/Failed 상태 관리 | `Update_TableStorage_Status_*` |

#### Retry Policy 설정

```json
{
  "retryPolicy": {
    "type": "exponential",
    "count": 3,
    "interval": "PT5S",
    "maximumInterval": "PT30S",
    "minimumInterval": "PT5S"
  }
}
```

### 2️⃣ Security (보안)

> **목표**: 데이터와 시스템을 위협으로부터 보호

#### 보안 점수: 9/10 (v2.4.0 기준)

#### 적용 사항

| 항목 | 구현 내용 | 위치 |
|------|----------|------|
| **Managed Identity** | Logic App MSI로 Azure 리소스 접근 | `logic-app.bicep` |
| **Key Vault** | ADO PAT 등 민감 정보 보안 저장 | `key-vault.bicep` |
| **Key Vault 런타임 조회** | 워크플로우 실행 시 MSI로 Key Vault에서 PAT 조회 | `workflow.json` |
| **Easy Auth** | HTTP Trigger에 Microsoft Entra ID 인증 적용 | Azure Portal |
| **secureData** | 민감 입출력 데이터 로그 마스킹 | `workflow.json` |
| **HTTPS 전용** | 모든 통신 TLS 1.2+ 강제 | `storage.bicep` |
| **OAuth 2.0** | Gmail, Teams, ADO 커넥터 OAuth 인증 | `api-connections.bicep` |
| **V1 Connection + MSI** | API Connection에 Logic App MSI로 접근 (Contributor 역할) | `api-connections.bicep` |

#### MSI 인증 예시

```json
{
  "authentication": {
    "type": "ManagedServiceIdentity",
    "audience": "https://storage.azure.com/"
  }
}
```

### 3️⃣ Cost Optimization (비용 최적화)

> **목표**: 클라우드 비용 효율성 극대화

#### 적용 사항

| 항목 | 구현 내용 | 예상 효과 |
|------|----------|----------|
| **WS1 Plan** | Workflow Standard 1 (최소 사양) | 월 ~$150 |
| **중복 체크** | 불필요한 AI 호출/Work Item 생성 방지 | AI 비용 절감 |
| **Standard LRS** | 개발 환경 스토리지 복제 최소화 | 스토리지 비용 절감 |
| **태그 기반 비용 추적** | CostCenter 태그 적용 | 비용 가시성 확보 |

### 4️⃣ Operational Excellence (운영 우수성)

> **목표**: 효율적인 운영과 지속적 개선

#### 적용 사항

| 항목 | 구현 내용 | 위치 |
|------|----------|------|
| **Scope 기반 구조** | 논리적 단위로 액션 그룹화 | `workflow.json` |
| **상세 로깅** | 모든 단계에서 구조화된 로그 출력 | `Log_*` 액션 |
| **에러 핸들링** | 중앙 집중식 에러 처리 Scope | `Scope_Error_Handler` |
| **IaC (Bicep)** | 인프라 코드화로 재현 가능한 배포 | `infra/` |
| **Application Insights** | 워크플로우 실행 모니터링 | `logic-app.bicep` |

#### Scope 구조

```
Scope_Initialize_Variables    → 변수 초기화
    ↓
Scope_Duplicate_Check        → 중복 체크
    ↓
Scope_Core_Processing        → 핵심 처리 (AI + ADO)
    ↓ (성공)                 ↓ (실패)
Scope_Notification           Scope_Error_Handler
```

### 5️⃣ Performance Efficiency (성능 효율성)

> **목표**: 시스템 요구사항을 효율적으로 충족

#### 적용 사항

| 항목 | 구현 내용 | 효과 |
|------|----------|------|
| **Stateful 워크플로우** | 장시간 실행 지원, 상태 지속 | 안정성 |
| **Chunked Transfer** | 대용량 데이터 스트리밍 처리 | 메모리 효율 |
| **1분 폴링** | Gmail 트리거 간격 최적화 | 적시성 vs 비용 균형 |
| **Null 안전 처리** | `coalesce()` 함수로 null 방지 | 런타임 오류 감소 |

## 📊 워크플로우 상세 설계

### 처리 흐름

```
1. Gmail 새 메일 도착
2. 변수 초기화 (null 안전 처리)
3. 중복 체크 (Table Storage 조회)
   - 중복: 로그 후 정상 종료
   - 신규: 계속 진행
4. 메일 ID 등록 (Status: Processing)
5. AI 분석 (Azure OpenAI GPT-4o)
6. ADO Work Item 생성
7. Work Item 필드 업데이트 (AssignedTo, Tags)
8. 처리 완료 상태 업데이트 (Status: Completed)
9. Teams 알림 전송
10. [에러 시] 에러 핸들러 실행
    - 에러 로깅
    - 상태 업데이트 (Status: Failed)
    - 에러 알림 전송
    - 워크플로우 실패 종료
```

### Table Storage 스키마

| 필드 | 타입 | 설명 |
|------|------|------|
| PartitionKey | string | `yyyy-MM` 형식 (월별 파티션) |
| RowKey | string | Gmail Message ID (URL 인코딩) |
| Subject | string | 이메일 제목 |
| SenderEmail | string | 발신자 이메일 |
| ReceivedTimeKST | string | 수신 시간 (KST) |
| ProcessedTime | datetime | 처리 시간 (UTC) |
| Source | string | `Gmail` (고정값) |
| Status | string | `Processing`, `Completed`, `Failed` |
| WorkItemId | int | 생성된 ADO Work Item ID |
| ErrorMessage | string | 에러 메시지 (실패 시) |

## 🔐 보안 고려사항

### 인증 흐름

```
┌─────────────┐     OAuth 2.0      ┌─────────────┐
│   Gmail     │◀─────────────────▶ │ API Conn.   │
└─────────────┘                    └──────┬──────┘
                                          │ Access Policy
                                          ▼
┌─────────────┐     MSI Auth       ┌─────────────┐
│ Logic App   │◀─────────────────▶ │ Azure AD    │
│   (MSI)     │                    └─────────────┘
└──────┬──────┘
       │ MSI Token
       ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ Table       │     │ Azure       │     │ Key Vault   │
│ Storage     │     │ OpenAI      │     │ (Secrets)   │
└─────────────┘     └─────────────┘     └─────────────┘
```

### Easy Auth (v2.3.0 → v2.4.0 비활성화)

> ⚠️ **v2.4.0**: Google Apps Script에서 HTTP 호출을 허용하기 위해 Easy Auth를 **비활성화**했습니다.
> URL의 SAS 서명(`sig` 파라미터)으로 보안을 유지합니다.

```powershell
# Easy Auth 상태 확인
az webapp auth show --name email2ado-logic-prod --resource-group rg-zb-taskman --query enabled
# 결과: false (비활성화됨)
```

**현재 인증 방식**:
```
┌─────────────┐     SAS 서명 (sig=...)    ┌─────────────────┐
│  Apps Script │──────────────────▶│  Email2ADO-HTTP │
│  (External)  │                    │  Workflow       │
└─────────────┘                    └─────────────────┘
```

- **보안 위험**: SAS 서명 노출 시 URL 재생성 필요
- **재생성 방법**: Logic App > Workflows > Email2ADO-HTTP > Overview > Regenerate URL

### Key Vault 런타임 조회 (v2.3.0+)

ADO PAT는 App Settings Key Vault Reference가 아닌, 워크플로우 실행 시점에 MSI로 직접 Key Vault에서 조회합니다.

```json
{
  "Get_ADO_PAT_From_KeyVault": {
    "type": "Http",
    "inputs": {
      "uri": "https://kv-zbtask-prod.vault.azure.net/secrets/ado-pat?api-version=7.4",
      "authentication": {
        "type": "ManagedServiceIdentity",
        "audience": "https://vault.azure.net"
      }
    },
    "runtimeConfiguration": {
      "secureData": { "properties": ["outputs"] }
    }
  }
}
```

이 방식의 장점:
- App Settings에 PAT 노출 안됨
- Key Vault 감사 로그에 접근 기록 남음
- 런타임 시 최신 PAT 사용 (Secret 교체 시 재배포 불필요)

### 민감 정보 관리

| 항목 | 저장 위치 | 접근 방식 | 비고 |
|------|----------|----------|------|
| ADO Work Item 생성 | API Connection (OAuth) | MSI + OAuth | PAT 불필요 |
| ADO 필드 업데이트 | **Key Vault** (ado-pat) | MSI 런타임 조회 | v2.3.0+ |
| Storage Key | App Settings | 연결 문자열 | MSI 전환 권장 |
| OAuth Tokens | API Connection | 자동 관리 | - |
| OpenAI 인증 | MSI | Cognitive Services User | API Key 불필요 |
| HTTP Trigger 인증 | SAS 서명 | URL sig= 파라미터 | v2.4.0 (비활성화) |

## 📚 참조 문서

- [Azure Well-Architected Framework](https://learn.microsoft.com/en-us/azure/well-architected/)
- [Logic Apps 에러 핸들링](https://learn.microsoft.com/en-us/azure/logic-apps/error-exception-handling)
- [Logic Apps Retry Policy](https://learn.microsoft.com/en-us/azure/logic-apps/logic-apps-exception-handling#retry-policies)
- [Gmail 커넥터](https://learn.microsoft.com/en-us/connectors/gmail/)

## 📝 변경 이력

| 버전 | 날짜 | 작성자 | 내용 |
|------|------|--------|------|
| 2.4.0 | 2026-01-31 | azure-mvp | Phase 11: Gmail 자동 연동 (Apps Script), Easy Auth 비활성화 |
| 2.3.0 | 2026-01-30 | azure-mvp | Phase 9: Key Vault 런타임 조회, Easy Auth, secureData 마스킹 |
| 2.2.1 | 2026-01-30 | azure-mvp | Phase 8: Email2ADO-HTTP 워크플로우, Power Automate Workflow 연동 |
| 2.2.0 | 2026-01-30 | azure-mvp | Phase 7: Key Vault 통합 (ADO PAT 보안 저장) |
| 2.1.0 | 2026-01-29 | azure-mvp | WAF 5 Pillars 기반 재설계 |
| 2.0.0 | 2026-01-29 | azure-mvp | Gmail 트리거로 전환 |
